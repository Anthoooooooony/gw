# Rust 生态过滤规则（v2 DSL：仅无损变换）。
# 这里只做 strip_ansi + 长度截断。去编译进度、只保留失败用例等语义压缩
# 需要写专属 Go filter（见 ROADMAP / ARCHITECTURE）。

# 注：`cargo build` / `cargo check` / `cargo clippy` / `cargo test` 均由
# filter/cargo 语义接管，不再在 TOML 维护规则。
